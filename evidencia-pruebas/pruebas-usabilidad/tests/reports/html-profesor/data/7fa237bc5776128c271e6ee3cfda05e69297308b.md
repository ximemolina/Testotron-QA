# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: grupos.accessibility.spec.ts >> Expulsión de miembro >> la confirmación de eliminación (confirm nativo) no rompe el flujo
- Location: evidencia-pruebas\pruebas-usabilidad\tests\grupos.accessibility.spec.ts:276:7

# Error details

```
Error: expect(received).toEqual(expected) // deep equality

- Expected  -  1
+ Received  + 61

- Array []
+ Array [
+   Object {
+     "description": "Ensure the contrast between foreground and background colors meets WCAG 2 AA minimum contrast ratio thresholds",
+     "help": "Elements must meet minimum color contrast ratio thresholds",
+     "helpUrl": "https://dequeuniversity.com/rules/axe/4.11/color-contrast?application=playwright",
+     "id": "color-contrast",
+     "impact": "serious",
+     "nodes": Array [
+       Object {
+         "all": Array [],
+         "any": Array [
+           Object {
+             "data": Object {
+               "bgColor": "#f8f9fa",
+               "contrastRatio": 4.26,
+               "expectedContrastRatio": "4.5:1",
+               "fgColor": "#0d6efd",
+               "fontSize": "12.0pt (16px)",
+               "fontWeight": "normal",
+               "messageKey": null,
+             },
+             "id": "color-contrast",
+             "impact": "serious",
+             "message": "Element has insufficient color contrast of 4.26 (foreground color: #0d6efd, background color: #f8f9fa, font size: 12.0pt (16px), font weight: normal). Expected contrast ratio of 4.5:1",
+             "relatedNodes": Array [
+               Object {
+                 "html": "<body class=\"bg-light\">",
+                 "target": Array [
+                   "body",
+                 ],
+               },
+             ],
+           },
+         ],
+         "failureSummary": "Fix any of the following:
+   Element has insufficient color contrast of 4.26 (foreground color: #0d6efd, background color: #f8f9fa, font size: 12.0pt (16px), font weight: normal). Expected contrast ratio of 4.5:1",
+         "html": "<button class=\"btn btn-outline-primary\" data-bs-toggle=\"modal\" data-bs-target=\"#editGroupModal\">
+     <i class=\"bi bi-pencil-square me-1\"></i>
+     Editar grupo
+   </button>",
+         "impact": "serious",
+         "none": Array [],
+         "target": Array [
+           ".btn-outline-primary",
+         ],
+       },
+     ],
+     "tags": Array [
+       "cat.color",
+       "wcag2aa",
+       "wcag143",
+       "TTv5",
+       "TT13.c",
+       "EN-301-549",
+       "EN-9.1.4.3",
+       "ACT",
+       "RGAAv4",
+       "RGAA-3.2.1",
+     ],
+   },
+ ]
```

# Page snapshot

```yaml
- generic [ref=e1]:
  - link "Saltar al contenido principal" [ref=e2] [cursor=pointer]:
    - /url: "#main-content"
  - generic [ref=e3]:
    - banner [ref=e4]:
      - text: 
      - navigation "Barra de usuario" [ref=e5]:
        - generic [ref=e6]:
          - generic [ref=e7]:
            - img "Logo de Testotron" [ref=e8]
            - generic [ref=e9]: Testotron
          - generic [ref=e10]:
            - generic [ref=e11]:
              - paragraph [ref=e12]: Hola de nuevo,
              - paragraph [ref=e13]: Juan García
            - 'generic "Rol: Profesor" [ref=e14]': Profesor
            - link "Ir a mi perfil" [ref=e15] [cursor=pointer]:
              - /url: /profile
              - generic [ref=e16]: JG
    - generic [ref=e17]:
      - navigation "Menú principal" [ref=e18]:
        - navigation "Menú de navegación principal" [ref=e19]:
          - list [ref=e20]:
            - listitem [ref=e21]:
              - link "Ir al panel de inicio" [ref=e22] [cursor=pointer]:
                - /url: /dashboard
                - generic [ref=e23]: 
                - text: Panel de inicio
            - listitem [ref=e24]:
              - link "Ir a mis cuestionarios" [ref=e25] [cursor=pointer]:
                - /url: /teacher/quizzes
                - generic [ref=e26]: 
                - text: Cuestionarios
            - listitem [ref=e27]:
              - link "Ir a resultados" [ref=e28] [cursor=pointer]:
                - /url: /teacher/results
                - generic [ref=e29]: 
                - text: Resultados
            - listitem [ref=e30]:
              - link "Ir a mis grupos" [ref=e31] [cursor=pointer]:
                - /url: /groups
                - generic [ref=e32]: 
                - text: Grupos
          - list [ref=e34]:
            - listitem [ref=e35]:
              - link "Ir a mi perfil" [ref=e36] [cursor=pointer]:
                - /url: /profile
                - generic [ref=e37]: 
                - text: Mi perfil
            - listitem [ref=e38]:
              - button "Cerrar sesión" [ref=e40] [cursor=pointer]:
                - generic [ref=e41]: 
                - text: Cerrar sesión
      - generic [ref=e42]:
        - main [ref=e43]:
          - main [ref=e44]:
            - generic [ref=e45]:
              - heading "Grupo01" [level=1] [ref=e46]
              - paragraph [ref=e47]: "Código de acceso: LBJ6617K"
              - paragraph
            - button " Editar grupo" [ref=e49] [cursor=pointer]:
              - generic [ref=e50]: 
              - text: Editar grupo
            - region "Estadísticas del grupo" [ref=e51]:
              - article [ref=e53]:
                - generic [ref=e54]:
                  - paragraph [ref=e55]: Miembros
                  - paragraph [ref=e56]: "1"
              - article [ref=e58]:
                - generic [ref=e59]:
                  - paragraph [ref=e60]: Cuestionarios
                  - paragraph [ref=e61]: "0"
              - article [ref=e63]:
                - generic [ref=e64]:
                  - paragraph [ref=e65]: Promedio general
                  - paragraph [ref=e66]: 0%
              - article [ref=e68]:
                - generic [ref=e69]:
                  - paragraph [ref=e70]: Código de acceso
                  - paragraph [ref=e71]: LBJ6617K
            - region "Miembros del grupo" [ref=e72]:
              - generic [ref=e73]:
                - heading "Miembros" [level=2] [ref=e74]
                - button " Incluir usuario" [ref=e75] [cursor=pointer]:
                  - generic [ref=e76]: 
                  - text: Incluir usuario
              - list [ref=e78]:
                - listitem [ref=e79]:
                  - generic [ref=e80]:
                    - strong [ref=e81]: Estudiante3
                    - text: student3@student.com
                  - generic [ref=e82]:
                    - generic [ref=e83]: student
                    - button "" [active] [ref=e85] [cursor=pointer]:
                      - generic [ref=e86]: 
            - region "Cuestionarios asignados" [ref=e87]:
              - heading "Cuestionarios asignados" [level=2] [ref=e89]
              - generic [ref=e90]:
                - list
        - contentinfo [ref=e91]:
          - generic [ref=e94]:
            - generic [ref=e95]: © Testotron
            - generic [ref=e96]:
              - link "Ayuda" [ref=e97] [cursor=pointer]:
                - /url: /help
                - generic [ref=e98]: 
                - text: Ayuda
              - link "Acerca de Testotron" [ref=e99] [cursor=pointer]:
                - /url: /about
                - generic [ref=e100]: 
                - text: Acerca de Testotron
```

# Test source

```ts
  191 | 
  192 | // ─── 5. Eliminación de grupo ──────────────────────────────────────────────────
  193 | test.describe('Eliminación de grupo', () => {
  194 |   test('el menú ⋮ de la tarjeta expone "Ver grupo" y "Borrar grupo"', async ({ authenticatedPage: page }) => {
  195 |     await page.goto(GRUPOS_URL);
  196 |     await page.waitForLoadState('networkidle');
  197 | 
  198 |     // El botón ⋮ tiene aria-label "Opciones del grupo <nombre>"
  199 |     await page.getByRole('button', { name: /opciones del grupo/i }).first().click();
  200 |     await page.waitForSelector('text=Borrar grupo', { state: 'visible' });
  201 | 
  202 |     const violations = await getViolations(
  203 |       new AxeBuilder({ page }).withTags(['wcag2aa'])
  204 |     );
  205 |     expect(violations.filter((v: Result) => v.id === 'button-name')).toEqual([]);
  206 |   });
  207 | 
  208 |   test('el diálogo de confirmación al borrar es accesible', async ({ authenticatedPage: page }) => {
  209 |     await page.goto(GRUPOS_URL);
  210 |     await page.waitForLoadState('networkidle');
  211 | 
  212 |     await page.getByRole('button', { name: /opciones del grupo/i }).first().click();
  213 |     await page.waitForSelector('text=Borrar grupo', { state: 'visible' });
  214 |     await page.getByText('Borrar grupo').click();
  215 | 
  216 |     await page.waitForSelector('[role="dialog"], [role="alertdialog"]', {
  217 |       state: 'visible', timeout: 3000,
  218 |     }).catch(() => {});
  219 | 
  220 |     const violations = await scanPage(page, 'Diálogo confirmación borrar grupo');
  221 |     expect(blocking(violations)).toEqual([]);
  222 |   });
  223 | });
  224 | 
  225 | // ─── 6. Incluir usuario (agregar miembro por correo) ─────────────────────────
  226 | test.describe('Incluir usuario al grupo', () => {
  227 |   test.beforeEach(async ({ authenticatedPage: page }) => {
  228 |     await page.goto(GRUPO_DETALLE_URL);
  229 |     await page.waitForLoadState('networkidle');
  230 |     // Botón "+ Incluir usuario" en la sección Miembros
  231 |     await page.getByRole('button', { name: /incluir usuario/i }).click();
  232 |     await page.waitForSelector('text=Agregar usuario al grupo', { state: 'visible' });
  233 |   });
  234 | 
  235 |   test('el modal "Agregar usuario al grupo" no tiene violaciones bloqueantes', async ({ authenticatedPage: page }) => {
  236 |     const violations = await scanPage(page, 'Modal — Agregar usuario al grupo');
  237 |     // heading-order: bug conocido — <h5> en modal debe cambiarse a <h3> (ver README)
  238 |     expect(blocking(violations).filter((v: Result) => v.id !== 'heading-order')).toEqual([]);
  239 |   });
  240 | 
  241 |   test('el campo "Correo electrónico" tiene label asociado al input (no solo texto suelto)', async ({ authenticatedPage: page }) => {
  242 |     const violations = await getViolations(
  243 |       new AxeBuilder({ page }).withTags(['wcag2aa'])
  244 |     );
  245 |     expect(violations.filter((v: Result) => v.id === 'label')).toEqual([]);
  246 |   });
  247 | 
  248 |   test('el campo de correo tiene autocomplete="email" (WCAG 1.3.5)', async ({ authenticatedPage: page }) => {
  249 |     // BUG: falta autocomplete="email" en el input — debe corregirse en el HTML
  250 |     const emailInput = page.locator('#addUserModal input[type="email"]');
  251 |     const autocomplete = await emailInput.getAttribute('autocomplete');
  252 |     expect(autocomplete).toBe('email'); // fallará hasta que se agregue el atributo
  253 |   });
  254 | 
  255 |   test('el botón "Agregar usuario" tiene nombre accesible', async ({ authenticatedPage: page }) => {
  256 |     const violations = await getViolations(
  257 |       new AxeBuilder({ page }).withTags(['wcag2aa'])
  258 |     );
  259 |     expect(violations.filter((v: Result) => v.id === 'button-name')).toEqual([]);
  260 |   });
  261 | });
  262 | 
  263 | // ─── 7. Expulsión de miembro ──────────────────────────────────────────────────
  264 | test.describe('Expulsión de miembro', () => {
  265 |   test('el botón de eliminar miembro (ícono basura) tiene aria-label', async ({ authenticatedPage: page }) => {
  266 |     await page.goto(GRUPO_DETALLE_URL);
  267 |     await page.waitForLoadState('networkidle');
  268 | 
  269 |     // BUG CONOCIDO: el botón de basura no tiene aria-label ni texto visible
  270 |     const violations = await getViolations(
  271 |       new AxeBuilder({ page }).withTags(['wcag2aa'])
  272 |     );
  273 |     expect(violations.filter((v: Result) => v.id === 'button-name')).toEqual([]);
  274 |   });
  275 | 
  276 |   test('la confirmación de eliminación (confirm nativo) no rompe el flujo', async ({ authenticatedPage: page }) => {
  277 |     await page.goto(GRUPO_DETALLE_URL);
  278 |     await page.waitForLoadState('networkidle');
  279 | 
  280 |     // Usar page.once para capturar el confirm() nativo y cancelarlo
  281 |     page.once('dialog', dialog => dialog.dismiss());
  282 | 
  283 |     const trashBtn = page.locator('form[action*="/members/remove"] button').first();
  284 |     const count = await trashBtn.count();
  285 |     if (count > 0) {
  286 |       await trashBtn.click();
  287 |       await page.waitForTimeout(500); // esperar que el diálogo nativo se resuelva
  288 |       const violations = await getViolations(
  289 |         new AxeBuilder({ page }).withTags(['wcag2aa'])
  290 |       );
> 291 |       expect(blocking(violations)).toEqual([]);
      |                                    ^ Error: expect(received).toEqual(expected) // deep equality
  292 |     }
  293 |   });
  294 | });
  295 | 
  296 | // ─── 8. Ingresar a grupo por código (rol Estudiante) ─────────────────────────
  297 | /*test.describe('Ingresar a grupo por código', () => {
  298 |   test.beforeEach(async ({ authenticatedPage: page }) => {
  299 |     await page.goto(GRUPOS_URL);
  300 |     await page.waitForLoadState('networkidle');
  301 |     // Botón "Ingresar a grupo" — visible solo para rol Estudiante
  302 |     const btn = page.getByRole('button', { name: /ingresar a grupo/i });
  303 |     const count = await btn.count();
  304 |     if (count > 0) {
  305 |       await btn.click();
  306 |       await page.waitForSelector('text=Ingresar a grupo', { state: 'visible' });
  307 |     } else {
  308 |       test.skip(); // el usuario actual es Profesor, no tiene este botón
  309 |     }
  310 |   });
  311 | 
  312 |   test('el modal "Ingresar a grupo" no tiene violaciones bloqueantes', async ({ authenticatedPage: page }) => {
  313 |     const violations = await scanPage(page, 'Modal — Ingresar a grupo');
  314 |     expect(blocking(violations)).toEqual([]);
  315 |   });
  316 | 
  317 |   test('el campo "Código del grupo" tiene label visible', async ({ authenticatedPage: page }) => {
  318 |     const label = page.getByText('Código del grupo');
  319 |     await expect(label).toBeVisible();
  320 | 
  321 |     const violations = await getViolations(
  322 |       new AxeBuilder({ page }).withTags(['wcag2aa'])
  323 |     );
  324 |     expect(violations.filter((v: Result) => v.id === 'label')).toEqual([]);
  325 |   });
  326 | 
  327 |   test('el botón "Ingresar" tiene nombre accesible', async ({ authenticatedPage: page }) => {
  328 |     const btn = page.getByRole('button', { name: 'Ingresar' });
  329 |     await expect(btn).toBeVisible();
  330 | 
  331 |     const violations = await getViolations(
  332 |       new AxeBuilder({ page }).withTags(['wcag2aa'])
  333 |     );
  334 |     expect(violations.filter((v: Result) => v.id === 'button-name')).toEqual([]);
  335 |   });
  336 | 
  337 |   test('código inválido muestra error accesible', async ({ authenticatedPage: page }) => {
  338 |     await page.locator('input').first().fill('CODIGO-INVALIDO-000');
  339 |     await page.getByRole('button', { name: 'Ingresar' }).click();
  340 | 
  341 |     await page.waitForSelector('[role="alert"], [aria-live]', {
  342 |       state: 'visible', timeout: 3000,
  343 |     }).catch(() => {});
  344 | 
  345 |     const violations = await getViolations(
  346 |       new AxeBuilder({ page }).withTags(['wcag2aa'])
  347 |     );
  348 |     expect(violations.filter((v: Result) =>
  349 |       ['aria-live-region-filler', 'aria-required-attr'].includes(v.id)
  350 |     )).toEqual([]);
  351 |   });
  352 | 
  353 |   test('el botón "Cancelar" cierra el modal', async ({ authenticatedPage: page }) => {
  354 |     await page.getByRole('button', { name: 'Cancelar' }).click();
  355 |     await page.waitForSelector('text=Ingresar a grupo', { state: 'hidden' }).catch(() => {});
  356 |     const focusedTag = await page.evaluate(() => document.activeElement?.tagName);
  357 |     expect(['BUTTON', 'A', 'INPUT', 'BODY']).toContain(focusedTag);
  358 |   });
  359 | });*/
  360 | 
  361 | // ─── 9. Navegación por teclado ────────────────────────────────────────────────
  362 | test.describe('Navegación por teclado', () => {
  363 |   test('todos los elementos interactivos son alcanzables con Tab', async ({ authenticatedPage: page }) => {
  364 |     await page.goto(GRUPOS_URL);
  365 |     await page.waitForLoadState('networkidle');
  366 | 
  367 |     const violations = await getViolations(
  368 |       new AxeBuilder({ page }).withTags(['wcag2aa'])
  369 |     );
  370 |     expect(violations.filter((v: Result) =>
  371 |       ['keyboard', 'focus-visible', 'tabindex'].includes(v.id)
  372 |     )).toEqual([]);
  373 |   });
  374 | 
  375 |   test('no hay trampas de foco', async ({ authenticatedPage: page }) => {
  376 |     await page.goto(GRUPOS_URL);
  377 |     await page.waitForLoadState('networkidle');
  378 | 
  379 |     const violations = await getViolations(
  380 |       new AxeBuilder({ page }).withTags(['wcag2aa'])
  381 |     );
  382 |     expect(violations.filter((v: Result) =>
  383 |       v.id === 'scrollable-region-focusable'
  384 |     )).toEqual([]);
  385 |   });
  386 | 
  387 |   test('la navegación lateral es accesible', async ({ authenticatedPage: page }) => {
  388 |     await page.goto(GRUPOS_URL);
  389 |     await page.waitForLoadState('networkidle');
  390 | 
  391 |     const violations = await getViolations(
```