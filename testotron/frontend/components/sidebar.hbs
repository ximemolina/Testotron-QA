<nav class="d-flex flex-column flex-shrink-0 p-3 bg-white h-100" style="width: 280px;" aria-label="Menú de navegación principal">
  <ul class="nav nav-pills flex-column mb-auto" role="list">

    <li class="nav-item" role="listitem">
      <a href="/dashboard" class="nav-link {{#if activePage.dashboard}}active{{else}}link-dark{{/if}}"
        {{#if activePage.dashboard}}aria-current="page"{{/if}}
        aria-label="Ir al panel de inicio">
        <i class="bi bi-house-door me-2" aria-hidden="true"></i> Panel de inicio
      </a>
    </li>

    {{!-- Menú para PROFESOR --}}
    {{#if (eq user.role 'teacher')}}
      <li role="listitem">
        <a href="/teacher/quizzes" class="nav-link {{#if activePage.quizzes}}active{{else}}link-dark{{/if}}"
          {{#if activePage.quizzes}}aria-current="page"{{/if}}
          aria-label="Ir a mis cuestionarios">
          <i class="bi bi-journal-text me-2" aria-hidden="true"></i> Cuestionarios
        </a>
      </li>
      <li role="listitem">
        <a href="/teacher/results" class="nav-link {{#if activePage.results}}active{{else}}link-dark{{/if}}"
          {{#if activePage.results}}aria-current="page"{{/if}}
          aria-label="Ir a resultados">
          <i class="bi bi-bar-chart me-2" aria-hidden="true"></i> Resultados
        </a>
      </li>
      <li role="listitem">
        <a href="/groups" class="nav-link {{#if activePage.groups}}active{{else}}link-dark{{/if}}"
          {{#if activePage.groups}}aria-current="page"{{/if}}
          aria-label="Ir a mis grupos">
          <i class="bi bi-people me-2" aria-hidden="true"></i> Grupos
        </a>
      </li>
    {{/if}}

    {{!-- Menú para ESTUDIANTE --}}
    {{#if (eq user.role 'student')}}
      <li role="listitem">
        <a href="/student/quizzes" class="nav-link {{#if activePage.myQuizzes}}active{{else}}link-dark{{/if}}"
          {{#if activePage.myQuizzes}}aria-current="page"{{/if}}
          aria-label="Ir a mis cuestionarios">
          <i class="bi bi-journal-text me-2" aria-hidden="true"></i> Cuestionarios
        </a>
      </li>
      <li role="listitem">
        <a href="/groups" class="nav-link {{#if activePage.groups}}active{{else}}link-dark{{/if}}"
          {{#if activePage.groups}}aria-current="page"{{/if}}
          aria-label="Ir a mis grupos">
          <i class="bi bi-people me-2" aria-hidden="true"></i> Grupos
        </a>
      </li>
    {{/if}}

    {{!-- Menú para ADMIN --}}
    {{#if (eq user.role 'admin')}}
      <li role="listitem">
        <a href="/admin/management" class="nav-link {{#if activePage.management}}active{{else}}link-dark{{/if}}"
          {{#if activePage.management}}aria-current="page"{{/if}}
          aria-label="Ir a administración">
          <i class="bi bi-shield-lock me-2" aria-hidden="true"></i> Administración
        </a>
      </li>
      <li role="listitem">
        <a href="/groups" class="nav-link {{#if activePage.groups}}active{{else}}link-dark{{/if}}"
          {{#if activePage.groups}}aria-current="page"{{/if}}
          aria-label="Ir a grupos">
          <i class="bi bi-people me-2" aria-hidden="true"></i> Grupos
        </a>
      </li>
    {{/if}}

  </ul>

  <!-- Cuenta -->
  <div class="mt-auto pt-3 border-top">
    <ul class="nav nav-pills flex-column" role="list">
      <li role="listitem">
        <a href="/profile" class="nav-link d-flex align-items-center {{#if activePage.profile}}active{{else}}link-dark{{/if}}"
          {{#if activePage.profile}}aria-current="page"{{/if}}
          aria-label="Ir a mi perfil">
          <i class="bi bi-person-circle me-2" aria-hidden="true"></i> Mi perfil
        </a>
      </li>
      <li role="listitem">
        <form action="/api/logout" method="POST">
          <button type="submit" class="nav-link link-danger d-flex align-items-center w-100 border-0 bg-transparent"
            aria-label="Cerrar sesión">
            <i class="bi bi-box-arrow-right me-2" aria-hidden="true"></i> Cerrar sesión
          </button>
        </form>
      </li>
    </ul>
  </div>
</nav>
